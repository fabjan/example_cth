on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.0.0
      - uses: erlef/setup-beam@v1.13.0
        with:
          otp-version: "25.1.2"
          rebar3-version: "3.20.0"
      - run: rebar3 ct
